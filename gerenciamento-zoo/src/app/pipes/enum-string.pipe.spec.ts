import { EnumStringPipe } from './enum-string.pipe';

describe('EnumStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumStringPipe();
    expect(pipe).toBeTruthy();
  });
});
